#!/bin/bash
command_file=$(mktemp -t "dii.command")
echo "$*" > $command_file
chmod +x $command_file
echo "Command: $command_file"
echo "$*"

find . -name 'Dockerfile' -exec dirname {} \; | xargs -I % sh -c "echo; echo '$(tput setaf 2)--- % ---$(tput sgr0)'; cd % && $command_file"
