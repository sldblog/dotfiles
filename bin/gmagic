#!/bin/bash
gii 'git checkout master && gu && if [ "$(git ls-files Gemfile.lock)" = "Gemfile.lock" ]; then git checkout Gemfile.lock; fi && git merge --ff-only && if [ -f Gemfile ]; then rm -f .bundle/config && bundle --jobs=4; fi && git submodule update'
